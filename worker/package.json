{
  "name": "ubl-flagship-edge",
  "version": "1.0.0",
  "description": "UBL Flagship Edge Worker with TDLN WASM",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "npm run build:wasm && esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js",
    "build:wasm": "cd ../tdln-core && cargo build --target wasm32-wasi --release && cp target/wasm32-wasi/release/tdln_core.wasm ../worker/dist/tdln_core.wasm"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240101.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.2",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
